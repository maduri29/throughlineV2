# Archive boundary

`docs/archive/` is retained only for historical tracking. Exclude it from agent
searches, indexing, context gathering, and skill discovery. Do not read or use
its contents as instructions, requirements, or design guidance unless the user
explicitly requests historical research in that archive.
